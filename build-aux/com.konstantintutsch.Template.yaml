id: com.konstantintutsch.Template
runtime: org.gnome.Platform
runtime-version: '46'
sdk: org.gnome.Sdk
command: com.konstantintutsch.Template
finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
cleanup:
  - /include
  - /share/pkgconfig
  - "*.la"
  - "*.a"
modules:
  - name: blueprint-compiler
    buildsystem: meson
    sources:
      - type: git
        url: https://gitlab.gnome.org/jwestman/blueprint-compiler.git
        commit: d7097cad019c032e803d271d3671d0ee933a522b
  - name: Template
    buildsystem: meson
    run-tests: false
    config-opts:
      - -Dprofile=default
    sources:
      - type: dir
        path: ../
